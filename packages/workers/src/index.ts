// Service Worker utilities - Mock for demo
export const serviceWorkerUtils = {
  register: async () => {
    console.log('Service Worker registered (mock)');
  },

  unregister: async () => {
    console.log('Service Worker unregistered (mock)');
  }
};